# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.pytest_cache/
.mypy_cache/

# Env
.env
.env.local

# Data and trained models (regeneratable)
data/*.db
data/*.csv
data/*.parquet
data/*.json
!data/.gitkeep
models/
*.pkl
*.joblib

# Jupyter
.ipynb_checkpoints/

# Node / Next.js
node_modules/
.next/
out/
next-env.d.ts

# IDE / OS
.vscode/
.idea/
*.swp
.DS_Store
Thumbs.db

# Deploy: ship the trained model + the metrics it serves and the real-data DB,
# so the Render demo is self-contained (no token, no build-time ingest).
# These override `models/`, `*.joblib`, and `data/*.db` above.
!backend/models/
!backend/models/xgb_calibrated.joblib
!backend/models/metrics.json
