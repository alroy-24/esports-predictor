# Slim runtime deps for the deployed API (no Jupyter / plotting / scraping /
# training extras). Used by the root Dockerfile. Keep in sync with the relevant
# pins in requirements.txt.
fastapi==0.115.0
uvicorn[standard]==0.32.0
pydantic==2.9.2
python-dotenv==1.0.1

# Data + DB
sqlalchemy==2.0.36
pandas==2.2.3
numpy==2.1.2

# ML (serve-time: predict + SHAP)
scikit-learn==1.5.2
xgboost==2.1.2
shap==0.46.0
joblib==1.4.2
